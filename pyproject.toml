[project]
name = "ato-a"
version = "0.1.0"
description = "Auto DWG from Excel — exhibition layout generator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ezdxf>=1.4.4",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "pillow>=12.3.0",
    "pyyaml>=6.0.3",
]
